/**
 * (tabs)/explorar.tsx
 *
 * Tela principal "Explorar" — aba gerenciada pelo Tab Navigator nativo.
 */

import ExplorarContent from '../../components/home/ExplorarContent';

export default function ExplorarScreen() {
  return <ExplorarContent />;
}
